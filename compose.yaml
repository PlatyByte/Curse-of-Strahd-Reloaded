---
services:
  foundry:
    image: ghcr.io/felddy/foundryvtt:14
    hostname: my_foundry_host
    userns_mode: keep-id
    volumes:
      - type: bind
        source: foundry-data
        target: /data
    environment:
      - FOUNDRY_USERNAME=platybyte
      - FOUNDRY_PASSWORD=)NHEFkLiZdRul;>F<[bm@H1gBQ
      - FOUNDRY_ADMIN_KEY=atropos
      - FOUNDRY_TELEMETRY=false
    ports:
      - target: 30000
        published: 30000
        protocol: tcp
